@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="card">
    <h2>Something went wrong.</h2>
    <p>Use the request id below if you need to trace the issue.</p>
    @if (Model.ShowRequestId)
    {
        <p><strong>Request ID:</strong> @Model.RequestId</p>
    }
</div>
